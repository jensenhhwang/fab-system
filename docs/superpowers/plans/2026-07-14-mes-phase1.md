# MES Phase 1 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ERP 시뮬레이터와 WMS에 연결되는 MES(공정 실행 관리) 1차 버전을 구축한다 — BOM 기반 작업지시, 반자동 FEFO 피킹, 공정-자재 준비도 매트릭스 포함.

**Architecture:** WorkOrder가 MES의 뼈대다. 작업지시 생성 시 BomTemplate에서 공정별 자재 소요량을 가져와 bomLines를 초기화한다. MATERIAL_WAIT 상태에서 담당자가 피킹 확정 버튼을 누르면 `computeFefo()`로 FEFO 순 lot을 선택하고 기존 `/api/lots/[id]/consume`을 호출해 InventoryMovement를 기록한다. 공정-자재 준비도 매트릭스는 processUsage + inventoryLots를 조인해 DOH를 실시간 계산한다.

**Tech Stack:** Next.js App Router, MongoDB native driver, TypeScript, Tailwind CSS, `src/lib/fefo.ts` (computeFefo), `src/lib/db.ts` (collections)

## Global Constraints

- 서버 컴포넌트에서 자기 API 라우트 HTTP 호출 금지 — `collections()` 직접 쿼리
- 스타일: white card `rounded-2xl shadow-sm`, accent `#0078D4`, 위험 `#EA002C`, 경고 `#F7A600`, 정상 `#00B96B`
- DOH 색상 규칙: < 5일 빨강(`#EA002C` 연하게), 5~ROP일 주황(`#F7A600`), ROP일 이상 초록(`#00B96B`), 데이터 없음 회색
- 피킹 확정은 ROLE = "MATERIALS" | "ADMIN" 만 가능 — 다른 role은 버튼 비활성화 + tooltip
- WorkOrder ID 형식: `"WO-{Date.now()}"`
- BomTemplate ID 형식: `"{processCode}-{product}"` (예: `"ETCH-HBM"`)
- 모든 API route에 `export const dynamic = "force-dynamic"` 선언
- `as never` 패턴 사용 금지 — 타입에 필드 명시적으로 추가해서 해결

---

## 파일 구조

```
src/lib/
  db.ts                                    ← 수정: WorkOrderDoc, BomTemplateDoc 타입 추가

src/app/api/mes/
  bom-templates/route.ts                   ← 신규: GET /api/mes/bom-templates
  process-readiness/route.ts               ← 신규: GET /api/mes/process-readiness
  workorders/route.ts                      ← 신규: GET + POST /api/mes/workorders
  workorders/[id]/status/route.ts          ← 신규: PATCH 상태 전이
  workorders/[id]/pick/route.ts            ← 신규: POST 피킹 확정

scripts/
  seed-bom-templates.ts                    ← 신규: processUsage 기반 초기 BomTemplate 시딩

src/app/(dashboard)/
  layout.tsx                               ← 수정: NAV에 "생산 실행" 그룹 추가
  mes/
    page.tsx                               ← 신규: 서버 컴포넌트 (초기 데이터 패칭)
    MesClient.tsx                          ← 신규: 3탭 클라이언트 컴포넌트
    ProcessReadinessMatrix.tsx             ← 신규: 공정-자재 DOH 매트릭스
    WorkOrderTable.tsx                     ← 신규: 작업지시 테이블 + 상태 배지
    WorkOrderCreateModal.tsx               ← 신규: 작업지시 생성 모달
    PickingDrawer.tsx                      ← 신규: 반자동 피킹 드로어
```

---

### Task 1: DB 스키마 확장

**Files:**
- Modify: `src/lib/db.ts`

**Interfaces:**
- Produces: `WorkOrderDoc`, `BomTemplateDoc`, `BomLine`, `PickedLot` 타입 + `collections()` 반환에 2개 컬렉션 추가

- [ ] **Step 1: `src/lib/db.ts`에 타입 추가**

`SimCheckpointDoc` 인터페이스 바로 아래(`export interface SimCheckpointDoc { ... }` 다음 줄)에 추가:

```ts
export interface PickedLot {
  lotId: string;
  qty: number;
}

export interface BomLine {
  materialId: string;
  plannedQty: number;
  actualQty?: number;
  pickedLots: PickedLot[];
}

export interface BomTemplateDoc {
  _id: string; // "{processCode}-{product}"
  processCode: string;
  product: Product;
  lines: { materialId: string; qtyPerRun: number }[];
  updatedAt: Date;
}

export type WorkOrderStatus = "QUEUED" | "MATERIAL_WAIT" | "RUNNING" | "DONE" | "HOLD";

export interface WorkOrderDoc {
  _id: string; // "WO-{Date.now()}"
  processCode: string;
  product: Product;
  plannedQty: number;
  status: WorkOrderStatus;
  bomLines: BomLine[];
  plannedStart?: Date;
  actualStart?: Date;
  actualEnd?: Date;
  createdBy: string;
  createdAt: Date;
  updatedAt: Date;
  note?: string;
}
```

- [ ] **Step 2: `collections()` 반환 타입에 추가**

반환 타입 블록(`simCheckpoints: Collection<SimCheckpointDoc>;` 다음 줄)에:

```ts
  bomTemplates: Collection<BomTemplateDoc>;
  workOrders: Collection<WorkOrderDoc>;
```

구현 블록(`simCheckpoints: db.collection<SimCheckpointDoc>("simCheckpoints"),` 다음 줄)에:

```ts
    bomTemplates: db.collection<BomTemplateDoc>("bomTemplates"),
    workOrders: db.collection<WorkOrderDoc>("workOrders"),
```

- [ ] **Step 3: TypeScript 컴파일 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1 | head -30
```

Expected: 에러 없음 (또는 기존과 동일한 에러만)

- [ ] **Step 4: 커밋**

```bash
git add src/lib/db.ts
git commit -m "feat(mes): add WorkOrderDoc and BomTemplateDoc types to db.ts"
```

---

### Task 2: BOM 시드 스크립트 + BOM Template API

**Files:**
- Create: `scripts/seed-bom-templates.ts`
- Create: `src/app/api/mes/bom-templates/route.ts`

**Interfaces:**
- Consumes: `ProcessUsageDoc`, `BomTemplateDoc` (Task 1), `collections()`
- Produces: `GET /api/mes/bom-templates` → `BomTemplateDoc[]`

- [ ] **Step 1: `scripts/seed-bom-templates.ts` 작성**

processUsage의 monthlyQty / 30 = 하루 1런 기준 소요량으로 qtyPerRun 초기화.

```ts
import { collections } from "../src/lib/db";
import type { BomTemplateDoc } from "../src/lib/db";

async function main() {
  const { processUsage, bomTemplates } = await collections();
  const usages = await processUsage.find({}).toArray();

  // (processCode, product) 조합별로 그룹핑
  const map = new Map<string, { processCode: string; product: string; lines: { materialId: string; qtyPerRun: number }[] }>();

  for (const u of usages) {
    const key = `${u.processCode}-${u.product}`;
    if (!map.has(key)) {
      map.set(key, { processCode: u.processCode, product: u.product, lines: [] });
    }
    map.get(key)!.lines.push({
      materialId: u.materialId,
      qtyPerRun: Math.round((u.monthlyQty / 30) * 100) / 100,
    });
  }

  let upserted = 0;
  for (const [id, template] of map) {
    const doc: BomTemplateDoc = {
      _id: id,
      processCode: template.processCode,
      product: template.product as BomTemplateDoc["product"],
      lines: template.lines,
      updatedAt: new Date(),
    };
    await bomTemplates.replaceOne({ _id: id }, doc, { upsert: true });
    upserted++;
  }

  console.log(`✅ BOM 템플릿 ${upserted}개 시딩 완료`);
  process.exit(0);
}

main().catch(e => { console.error(e); process.exit(1); });
```

- [ ] **Step 2: 시드 스크립트 실행**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsx scripts/seed-bom-templates.ts
```

Expected: `✅ BOM 템플릿 N개 시딩 완료`

- [ ] **Step 3: `src/app/api/mes/bom-templates/route.ts` 작성**

```ts
import { NextResponse } from "next/server";
import { collections } from "@/lib/db";

export const dynamic = "force-dynamic";

export async function GET() {
  const { bomTemplates } = await collections();
  const templates = await bomTemplates.find({}).sort({ processCode: 1 }).toArray();
  return NextResponse.json(templates);
}
```

- [ ] **Step 4: API 확인**

```bash
curl http://localhost:3000/api/mes/bom-templates
```

Expected: `[{"_id":"ETCH-HBM","processCode":"ETCH","product":"HBM","lines":[...]},...]`

- [ ] **Step 5: TypeScript 컴파일 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 6: 커밋**

```bash
git add scripts/seed-bom-templates.ts src/app/api/mes/bom-templates/route.ts
git commit -m "feat(mes): add BOM template seed script and GET API"
```

---

### Task 3: 공정-자재 준비도 API

**Files:**
- Create: `src/app/api/mes/process-readiness/route.ts`

**Interfaces:**
- Consumes: `ProcessUsageDoc`, `InventoryLotDoc`, `MaterialDoc` (Task 1), `collections()`
- Produces: `GET /api/mes/process-readiness` → `ProcessReadinessRow[]`

```ts
// 응답 타입 (route 파일 안에 정의)
type ProcessReadinessCell = {
  materialId: string;
  materialName: string;
  dailyUsage: number;   // monthlyQty / 30
  availableQty: number; // 현재 가용 재고 합계
  doh: number;          // availableQty / dailyUsage (일 단위)
  ropDays: number;      // MaterialDoc.ropDays
};

type ProcessReadinessRow = {
  processCode: string;
  product: Product;
  cells: ProcessReadinessCell[];
};
```

- [ ] **Step 1: `src/app/api/mes/process-readiness/route.ts` 작성**

```ts
import { NextResponse } from "next/server";
import { collections } from "@/lib/db";
import type { Product } from "@/lib/db";

export const dynamic = "force-dynamic";

type ProcessReadinessCell = {
  materialId: string;
  materialName: string;
  dailyUsage: number;
  availableQty: number;
  doh: number;
  ropDays: number;
};

type ProcessReadinessRow = {
  processCode: string;
  product: Product;
  cells: ProcessReadinessCell[];
};

export async function GET() {
  const { processUsage, inventoryLots, materials } = await collections();

  const [usages, lots, mats] = await Promise.all([
    processUsage.find({}).toArray(),
    inventoryLots.find({ qualityStatus: "AVAILABLE" }).toArray(),
    materials.find({}).toArray(),
  ]);

  const matMap = new Map(mats.map(m => [m._id, m]));

  // 자재별 가용 재고 합산
  const availMap = new Map<string, number>();
  for (const lot of lots) {
    availMap.set(lot.materialId, (availMap.get(lot.materialId) ?? 0) + lot.availableQuantity);
  }

  // (processCode, product) 그룹핑
  const rowMap = new Map<string, ProcessReadinessRow>();
  for (const u of usages) {
    const key = `${u.processCode}-${u.product}`;
    if (!rowMap.has(key)) {
      rowMap.set(key, { processCode: u.processCode, product: u.product as Product, cells: [] });
    }
    const mat = matMap.get(u.materialId);
    const dailyUsage = u.monthlyQty / 30;
    const availableQty = availMap.get(u.materialId) ?? 0;
    const doh = dailyUsage > 0 ? Math.round(availableQty / dailyUsage) : 0;
    rowMap.get(key)!.cells.push({
      materialId: u.materialId,
      materialName: mat?.name ?? u.materialId,
      dailyUsage,
      availableQty,
      doh,
      ropDays: mat?.ropDays ?? 7,
    });
  }

  const rows = Array.from(rowMap.values()).sort((a, b) =>
    a.processCode.localeCompare(b.processCode)
  );

  return NextResponse.json(rows);
}
```

- [ ] **Step 2: API 확인**

```bash
curl http://localhost:3000/api/mes/process-readiness | python3 -m json.tool | head -40
```

Expected: `[{"processCode":"ETCH","product":"HBM","cells":[{"materialId":"CHM-007","doh":12,...}]}]`

- [ ] **Step 3: TypeScript 컴파일 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 4: 커밋**

```bash
git add src/app/api/mes/process-readiness/route.ts
git commit -m "feat(mes): add process readiness API with DOH computation"
```

---

### Task 4: WorkOrder CRUD + 상태 전이 API

**Files:**
- Create: `src/app/api/mes/workorders/route.ts`
- Create: `src/app/api/mes/workorders/[id]/status/route.ts`

**Interfaces:**
- Consumes: `WorkOrderDoc`, `BomTemplateDoc` (Task 1), `collections()`
- Produces:
  - `GET /api/mes/workorders?status=QUEUED,RUNNING` → `WorkOrderDoc[]`
  - `POST /api/mes/workorders` body `{processCode, product, plannedQty, plannedStart?, note?}` → `WorkOrderDoc`
  - `PATCH /api/mes/workorders/[id]/status` body `{status, note?}` → `WorkOrderDoc`

허용 상태 전이:
- `QUEUED` → `MATERIAL_WAIT` | `RUNNING` | `HOLD`
- `MATERIAL_WAIT` → `QUEUED` | `HOLD`
- `RUNNING` → `DONE` | `HOLD`
- `HOLD` → `QUEUED`

- [ ] **Step 1: `src/app/api/mes/workorders/route.ts` 작성**

```ts
import { NextRequest, NextResponse } from "next/server";
import { collections } from "@/lib/db";
import type { WorkOrderDoc, BomLine, Product } from "@/lib/db";

export const dynamic = "force-dynamic";

export async function GET(req: NextRequest) {
  const { searchParams } = req.nextUrl;
  const statusParam = searchParams.get("status");
  const { workOrders } = await collections();

  const filter = statusParam
    ? { status: { $in: statusParam.split(",") as WorkOrderDoc["status"][] } }
    : {};

  const orders = await workOrders
    .find(filter)
    .sort({ createdAt: -1 })
    .limit(100)
    .toArray();

  return NextResponse.json(orders);
}

export async function POST(req: NextRequest) {
  const body = await req.json();
  const { processCode, product, plannedQty, plannedStart, note, createdBy = "system" } = body as {
    processCode: string;
    product: Product;
    plannedQty: number;
    plannedStart?: string;
    note?: string;
    createdBy?: string;
  };

  if (!processCode || !product || !plannedQty || plannedQty <= 0) {
    return NextResponse.json({ error: "processCode, product, plannedQty 필수" }, { status: 400 });
  }

  const { workOrders, bomTemplates } = await collections();

  const templateId = `${processCode}-${product}`;
  const template = await bomTemplates.findOne({ _id: templateId });

  const bomLines: BomLine[] = (template?.lines ?? []).map(line => ({
    materialId: line.materialId,
    plannedQty: Math.round(line.qtyPerRun * plannedQty * 100) / 100,
    pickedLots: [],
  }));

  const now = new Date();
  const wo: WorkOrderDoc = {
    _id: `WO-${Date.now()}`,
    processCode,
    product,
    plannedQty,
    status: "QUEUED",
    bomLines,
    plannedStart: plannedStart ? new Date(plannedStart) : undefined,
    createdBy,
    createdAt: now,
    updatedAt: now,
    note,
  };

  await workOrders.insertOne(wo);
  return NextResponse.json(wo, { status: 201 });
}
```

- [ ] **Step 2: `src/app/api/mes/workorders/[id]/status/route.ts` 작성**

```ts
import { NextRequest, NextResponse } from "next/server";
import { collections } from "@/lib/db";
import type { WorkOrderStatus } from "@/lib/db";

export const dynamic = "force-dynamic";

const ALLOWED_TRANSITIONS: Record<WorkOrderStatus, WorkOrderStatus[]> = {
  QUEUED:        ["MATERIAL_WAIT", "RUNNING", "HOLD"],
  MATERIAL_WAIT: ["QUEUED", "HOLD"],
  RUNNING:       ["DONE", "HOLD"],
  HOLD:          ["QUEUED"],
  DONE:          [],
};

export async function PATCH(
  req: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params;
  const body = await req.json();
  const { status: nextStatus } = body as { status: WorkOrderStatus; note?: string };

  const { workOrders } = await collections();
  const wo = await workOrders.findOne({ _id: id });
  if (!wo) return NextResponse.json({ error: "WO 없음" }, { status: 404 });

  const allowed = ALLOWED_TRANSITIONS[wo.status] ?? [];
  if (!allowed.includes(nextStatus)) {
    return NextResponse.json(
      { error: `${wo.status} → ${nextStatus} 전이 불가` },
      { status: 409 }
    );
  }

  const now = new Date();
  const setFields: Partial<WorkOrderDoc> & { updatedAt: Date } = { status: nextStatus, updatedAt: now };
  if (nextStatus === "RUNNING" && !wo.actualStart) setFields.actualStart = now;
  if (nextStatus === "DONE") setFields.actualEnd = now;

  await workOrders.updateOne({ _id: id }, { $set: setFields });
  const updated = await workOrders.findOne({ _id: id });
  return NextResponse.json(updated);
}

// WorkOrderDoc을 import하기 위해 필요
import type { WorkOrderDoc } from "@/lib/db";
```

- [ ] **Step 3: API 확인**

```bash
# WO 생성
curl -X POST http://localhost:3000/api/mes/workorders \
  -H "Content-Type: application/json" \
  -d '{"processCode":"ETCH","product":"HBM","plannedQty":5}'
```

Expected: `{"_id":"WO-...","status":"QUEUED","bomLines":[...],...}`

```bash
# WO 목록
curl http://localhost:3000/api/mes/workorders
```

Expected: 배열, QUEUED WO 포함

```bash
# 상태 전이 (WO ID는 위 생성 결과에서 복사)
curl -X PATCH http://localhost:3000/api/mes/workorders/WO-XXXXX/status \
  -H "Content-Type: application/json" \
  -d '{"status":"RUNNING"}'
```

Expected: `{"status":"RUNNING","actualStart":"..."}` 포함 WO 반환

- [ ] **Step 4: TypeScript 컴파일 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 5: 커밋**

```bash
git add src/app/api/mes/workorders/route.ts "src/app/api/mes/workorders/[id]/status/route.ts"
git commit -m "feat(mes): add WorkOrder CRUD and status transition APIs"
```

---

### Task 5: 피킹 확정 API

**Files:**
- Create: `src/app/api/mes/workorders/[id]/pick/route.ts`

**Interfaces:**
- Consumes: `WorkOrderDoc`, `BomLine`, `InventoryLotDoc`, `collections()`, `computeFefo()` from `@/lib/fefo`
- Produces: `POST /api/mes/workorders/[id]/pick` body `{materialId, lotId, qty}` → `{ok, remainingAvailable, woStatus}`

피킹 흐름:
1. WO 조회 → status QUEUED 또는 MATERIAL_WAIT 확인
2. 해당 materialId의 bomLine 조회
3. inventoryLots에서 lot 조회 → FEFO 후보 계산
4. `qty`만큼 lot.availableQuantity 차감, inventoryMovements에 ISSUE 기록
5. bomLine.actualQty 누적, pickedLots에 {lotId, qty} 추가
6. 해당 bomLine의 actualQty >= plannedQty이면 "충족"
7. 모든 bomLine 충족 → WO status → QUEUED (실행 대기)

- [ ] **Step 1: `src/app/api/mes/workorders/[id]/pick/route.ts` 작성**

```ts
import { NextRequest, NextResponse } from "next/server";
import { collections } from "@/lib/db";
import { randomUUID } from "crypto";

export const dynamic = "force-dynamic";

export async function POST(
  req: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params;
  const body = await req.json();
  const { materialId, lotId, qty } = body as {
    materialId: string;
    lotId: string;
    qty: number;
  };

  if (!materialId || !lotId || !qty || qty <= 0) {
    return NextResponse.json({ error: "materialId, lotId, qty 필수" }, { status: 400 });
  }

  const { workOrders, inventoryLots, inventoryMovements } = await collections();

  const wo = await workOrders.findOne({ _id: id });
  if (!wo) return NextResponse.json({ error: "WO 없음" }, { status: 404 });
  if (wo.status !== "QUEUED" && wo.status !== "MATERIAL_WAIT") {
    return NextResponse.json({ error: "피킹 불가 상태: " + wo.status }, { status: 409 });
  }

  const bomLine = wo.bomLines.find(l => l.materialId === materialId);
  if (!bomLine) return NextResponse.json({ error: "BOM에 없는 자재" }, { status: 400 });

  const lot = await inventoryLots.findOne({ _id: lotId });
  if (!lot) return NextResponse.json({ error: "Lot 없음" }, { status: 404 });
  if (lot.availableQuantity < qty) {
    return NextResponse.json({ error: `가용 수량 부족 (가용: ${lot.availableQuantity})` }, { status: 409 });
  }

  const now = new Date();
  const newAvailable = lot.availableQuantity - qty;

  // Lot 차감
  await inventoryLots.updateOne(
    { _id: lotId },
    {
      $set: {
        availableQuantity: newAvailable,
        qualityStatus: newAvailable === 0 ? "CONSUMED" : lot.qualityStatus,
        updatedAt: now,
      },
    }
  );

  // InventoryMovement 기록
  await inventoryMovements.insertOne({
    _id: randomUUID(),
    materialId,
    type: "ISSUE",
    quantity: qty,
    lotId,
    processCode: wo.processCode,
    reason: `MES 피킹: ${id}`,
    userId: "mes-system",
    createdAt: now,
  });

  // WorkOrder bomLine 업데이트
  const updatedBomLines = wo.bomLines.map(line => {
    if (line.materialId !== materialId) return line;
    return {
      ...line,
      actualQty: (line.actualQty ?? 0) + qty,
      pickedLots: [...line.pickedLots, { lotId, qty }],
    };
  });

  // 모든 bomLine이 충족되면 QUEUED로 전이
  const allFulfilled = updatedBomLines.every(
    l => (l.actualQty ?? 0) >= l.plannedQty
  );
  const newStatus = allFulfilled ? "QUEUED" : wo.status;

  await workOrders.updateOne(
    { _id: id },
    { $set: { bomLines: updatedBomLines, status: newStatus, updatedAt: now } }
  );

  return NextResponse.json({
    ok: true,
    remainingAvailable: newAvailable,
    woStatus: newStatus,
    allFulfilled,
  });
}
```

- [ ] **Step 2: API 확인**

```bash
# 먼저 WO ID와 lotId를 확인
curl http://localhost:3000/api/mes/workorders | python3 -c "import sys,json; wos=json.load(sys.stdin); print(wos[0]['_id'] if wos else 'no WOs')"
curl http://localhost:3000/api/lots | python3 -c "import sys,json; lots=json.load(sys.stdin); print(lots[0]['_id'] if lots else 'no lots')" 2>/dev/null || echo "lots API 없음 — DB 직접 확인 필요"
```

WO와 lot ID를 확인 후:
```bash
curl -X POST http://localhost:3000/api/mes/workorders/WO-XXXXX/pick \
  -H "Content-Type: application/json" \
  -d '{"materialId":"CHM-007","lotId":"LOT-YYYYY","qty":10}'
```

Expected: `{"ok":true,"remainingAvailable":N,"woStatus":"QUEUED"|"MATERIAL_WAIT","allFulfilled":true|false}`

- [ ] **Step 3: TypeScript 컴파일 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 4: 커밋**

```bash
git add "src/app/api/mes/workorders/[id]/pick/route.ts"
git commit -m "feat(mes): add picking API with FEFO lot deduction and WO status update"
```

---

### Task 6: 사이드바 + 페이지 스캐폴드

**Files:**
- Modify: `src/app/(dashboard)/layout.tsx`
- Create: `src/app/(dashboard)/mes/page.tsx`
- Create: `src/app/(dashboard)/mes/MesClient.tsx`

**Interfaces:**
- Consumes: Tasks 3, 4 API 응답 타입
- Produces: `/mes` 라우트, 3탭 껍데기 (실제 내용은 Tasks 7-10에서 채움)

- [ ] **Step 1: `layout.tsx` NAV 배열에 "생산 실행" 그룹 추가**

`layout.tsx`에서 `{ group: "의사결정", ... }` 블록 바로 **앞에** 삽입:

```ts
  {
    group: "생산 실행",
    items: [
      { href: "/mes", label: "공정 실행 (MES)" },
    ],
  },
```

- [ ] **Step 2: `src/app/(dashboard)/mes/page.tsx` 작성**

```tsx
import { collections } from "@/lib/db";
import MesClient from "./MesClient";

export const dynamic = "force-dynamic";

export default async function MesPage() {
  const { workOrders, processUsage, inventoryLots, materials } = await collections();

  const [initialWorkOrders, usages, lots, mats] = await Promise.all([
    workOrders.find({}).sort({ createdAt: -1 }).limit(50).toArray(),
    processUsage.find({}).toArray(),
    inventoryLots.find({ qualityStatus: "AVAILABLE" }).toArray(),
    materials.find({}).toArray(),
  ]);

  return (
    <MesClient
      initialWorkOrders={JSON.parse(JSON.stringify(initialWorkOrders))}
    />
  );
}
```

- [ ] **Step 3: `src/app/(dashboard)/mes/MesClient.tsx` 작성 (껍데기)**

```tsx
"use client";

import { useState } from "react";
import type { WorkOrderDoc } from "@/lib/db";

type Tab = "readiness" | "workorders" | "log";

export default function MesClient({
  initialWorkOrders,
}: {
  initialWorkOrders: WorkOrderDoc[];
}) {
  const [tab, setTab] = useState<Tab>("readiness");
  const [workOrders, setWorkOrders] = useState<WorkOrderDoc[]>(initialWorkOrders);

  const TAB_LABELS: { key: Tab; label: string }[] = [
    { key: "readiness", label: "공정 준비 현황" },
    { key: "workorders", label: "작업지시 목록" },
    { key: "log", label: "실행 로그" },
  ];

  return (
    <div className="space-y-4">
      {/* 헤더 */}
      <div className="flex items-center justify-between">
        <h1 className="text-xl font-bold" style={{ color: "var(--text-1)" }}>
          공정 실행 관리 (MES)
        </h1>
        <button
          className="px-4 py-2 bg-[#0078D4] text-white text-sm font-medium rounded-lg hover:bg-blue-700"
          onClick={() => {}}
        >
          + 작업지시 생성
        </button>
      </div>

      {/* 탭 */}
      <div className="flex gap-1 border-b" style={{ borderColor: "var(--border)" }}>
        {TAB_LABELS.map(({ key, label }) => (
          <button
            key={key}
            onClick={() => setTab(key)}
            className={`px-4 py-2 text-sm font-medium border-b-2 transition-colors ${
              tab === key
                ? "border-[#0078D4] text-[#0078D4]"
                : "border-transparent hover:text-[#0078D4]"
            }`}
            style={{ color: tab === key ? "#0078D4" : "var(--text-2)" }}
          >
            {label}
          </button>
        ))}
      </div>

      {/* 탭 콘텐츠 (Tasks 7-10에서 채움) */}
      <div className="min-h-64 flex items-center justify-center text-sm" style={{ color: "var(--text-3)" }}>
        {tab === "readiness" && <span>공정 준비 현황 (Task 7에서 구현)</span>}
        {tab === "workorders" && <span>작업지시 목록 (Task 8에서 구현)</span>}
        {tab === "log" && <span>실행 로그 (Task 9에서 구현)</span>}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: 브라우저에서 확인**

`http://localhost:3000/mes` 접속.
- 사이드바에 "생산 실행 > 공정 실행 (MES)" 항목 표시 확인
- 3개 탭 클릭 전환 확인
- "+ 작업지시 생성" 버튼 표시 확인

- [ ] **Step 5: TypeScript 컴파일 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 6: 커밋**

```bash
git add "src/app/(dashboard)/layout.tsx" "src/app/(dashboard)/mes/page.tsx" "src/app/(dashboard)/mes/MesClient.tsx"
git commit -m "feat(mes): add MES page scaffold with 3-tab layout and sidebar nav"
```

---

### Task 7: ProcessReadinessMatrix 컴포넌트

**Files:**
- Create: `src/app/(dashboard)/mes/ProcessReadinessMatrix.tsx`
- Modify: `src/app/(dashboard)/mes/MesClient.tsx`

**Interfaces:**
- Consumes: `GET /api/mes/process-readiness` 응답
- Produces: `<ProcessReadinessMatrix />` — 2D 매트릭스 테이블, 셀 클릭 시 `onCellClick(processCode, materialId)` 콜백

```ts
// 사용할 타입
type ReadinessCell = {
  materialId: string;
  materialName: string;
  dailyUsage: number;
  availableQty: number;
  doh: number;
  ropDays: number;
};

type ReadinessRow = {
  processCode: string;
  product: string;
  cells: ReadinessCell[];
};
```

- [ ] **Step 1: `src/app/(dashboard)/mes/ProcessReadinessMatrix.tsx` 작성**

```tsx
"use client";

import { useEffect, useState } from "react";

type ReadinessCell = {
  materialId: string;
  materialName: string;
  dailyUsage: number;
  availableQty: number;
  doh: number;
  ropDays: number;
};

type ReadinessRow = {
  processCode: string;
  product: string;
  cells: ReadinessCell[];
};

function getDohColor(doh: number, ropDays: number): string {
  if (doh < 5) return "bg-red-100 text-red-700 font-semibold";
  if (doh < ropDays) return "bg-amber-100 text-amber-700";
  return "bg-green-100 text-green-700";
}

export default function ProcessReadinessMatrix({
  onCellClick,
}: {
  onCellClick: (processCode: string, product: string, materialId: string) => void;
}) {
  const [rows, setRows] = useState<ReadinessRow[]>([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetch("/api/mes/process-readiness")
      .then(r => r.json())
      .then(data => { setRows(data); setLoading(false); });
  }, []);

  if (loading) {
    return (
      <div className="bg-white rounded-2xl shadow-sm p-8 text-center text-sm" style={{ color: "var(--text-3)" }}>
        로딩 중...
      </div>
    );
  }

  // 전체 자재 목록 (열 헤더)
  const allMaterials = Array.from(
    new Map(
      rows.flatMap(r => r.cells.map(c => [c.materialId, c.materialName]))
    ).entries()
  ).map(([id, name]) => ({ id, name }));

  // KPI 집계
  const allCells = rows.flatMap(r => r.cells);
  const criticalCount = allCells.filter(c => c.doh < 5).length;
  const warningCount = allCells.filter(c => c.doh >= 5 && c.doh < c.ropDays).length;
  const okCount = allCells.filter(c => c.doh >= c.ropDays).length;

  return (
    <div className="space-y-4">
      {/* KPI 카드 */}
      <div className="grid grid-cols-3 gap-3">
        {[
          { label: "위험 (5일↓)", count: criticalCount, color: "border-red-400 bg-red-50", text: "text-red-700" },
          { label: "경고 (ROP 미달)", count: warningCount, color: "border-amber-400 bg-amber-50", text: "text-amber-700" },
          { label: "정상", count: okCount, color: "border-green-400 bg-green-50", text: "text-green-700" },
        ].map(({ label, count, color, text }) => (
          <div key={label} className={`rounded-2xl shadow-sm p-4 border-t-4 ${color}`}>
            <div className={`text-2xl font-bold ${text}`}>{count}</div>
            <div className="text-xs mt-1" style={{ color: "var(--text-3)" }}>{label}</div>
          </div>
        ))}
      </div>

      {/* 매트릭스 */}
      <div className="bg-white rounded-2xl shadow-sm overflow-auto">
        <table className="text-xs border-collapse w-full">
          <thead>
            <tr>
              <th className="sticky left-0 top-0 z-20 bg-gray-50 px-3 py-2 text-left font-semibold border-b border-r" style={{ color: "var(--text-2)", minWidth: 120 }}>
                공정 / 자재
              </th>
              {allMaterials.map(m => (
                <th key={m.id} className="sticky top-0 z-10 bg-gray-50 px-3 py-2 text-center font-medium border-b border-r whitespace-nowrap" style={{ color: "var(--text-2)", minWidth: 90 }}>
                  {m.name}
                </th>
              ))}
            </tr>
          </thead>
          <tbody>
            {rows.map(row => {
              const cellMap = new Map(row.cells.map(c => [c.materialId, c]));
              return (
                <tr key={`${row.processCode}-${row.product}`} className="hover:bg-gray-50">
                  <td className="sticky left-0 z-10 bg-white px-3 py-2 font-medium border-b border-r whitespace-nowrap" style={{ color: "var(--text-1)" }}>
                    {row.processCode}
                    <span className="ml-1.5 text-[10px] px-1.5 py-0.5 rounded bg-gray-100" style={{ color: "var(--text-3)" }}>
                      {row.product}
                    </span>
                  </td>
                  {allMaterials.map(m => {
                    const cell = cellMap.get(m.id);
                    if (!cell) {
                      return <td key={m.id} className="px-3 py-2 text-center border-b border-r text-gray-300">—</td>;
                    }
                    return (
                      <td
                        key={m.id}
                        className={`px-3 py-2 text-center border-b border-r cursor-pointer hover:opacity-80 ${getDohColor(cell.doh, cell.ropDays)}`}
                        onClick={() => onCellClick(row.processCode, row.product, m.id)}
                        title={`가용: ${cell.availableQty.toFixed(1)} / 일소비: ${cell.dailyUsage.toFixed(1)}`}
                      >
                        {cell.doh}일
                      </td>
                    );
                  })}
                </tr>
              );
            })}
          </tbody>
        </table>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: `MesClient.tsx`의 readiness 탭에 컴포넌트 연결**

`MesClient.tsx`에서:

import 추가:
```tsx
import ProcessReadinessMatrix from "./ProcessReadinessMatrix";
```

탭 콘텐츠 영역에서 readiness 탭 부분 교체:
```tsx
{tab === "readiness" && (
  <ProcessReadinessMatrix
    onCellClick={(processCode, product, materialId) => {
      console.log("셀 클릭:", processCode, product, materialId);
      // Task 9에서 PickingDrawer 연결
    }}
  />
)}
```

`div.min-h-64.flex.items-center.justify-center` 전체를 아래로 교체:
```tsx
<div>
  {tab === "readiness" && (
    <ProcessReadinessMatrix
      onCellClick={(processCode, product, materialId) => {
        console.log("셀 클릭:", processCode, product, materialId);
      }}
    />
  )}
  {tab === "workorders" && (
    <div className="text-sm text-center py-16" style={{ color: "var(--text-3)" }}>
      작업지시 목록 (Task 8에서 구현)
    </div>
  )}
  {tab === "log" && (
    <div className="text-sm text-center py-16" style={{ color: "var(--text-3)" }}>
      실행 로그 (Task 9에서 구현)
    </div>
  )}
</div>
```

- [ ] **Step 3: 브라우저에서 확인**

`http://localhost:3000/mes` → "공정 준비 현황" 탭:
- KPI 카드 3개(위험/경고/정상) 표시 확인
- 공정-자재 2D 매트릭스 표시 확인
- DOH 색상(빨강/주황/초록) 적용 확인
- 셀 클릭 시 콘솔에 `셀 클릭: ETCH HBM CHM-007` 출력 확인

- [ ] **Step 4: TypeScript 컴파일 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 5: 커밋**

```bash
git add "src/app/(dashboard)/mes/ProcessReadinessMatrix.tsx" "src/app/(dashboard)/mes/MesClient.tsx"
git commit -m "feat(mes): add ProcessReadinessMatrix with DOH color coding and KPI cards"
```

---

### Task 8: WorkOrderTable + WorkOrderCreateModal

**Files:**
- Create: `src/app/(dashboard)/mes/WorkOrderTable.tsx`
- Create: `src/app/(dashboard)/mes/WorkOrderCreateModal.tsx`
- Modify: `src/app/(dashboard)/mes/MesClient.tsx`

**Interfaces:**
- Consumes: `WorkOrderDoc`, `GET /api/mes/workorders`, `POST /api/mes/workorders`, `PATCH /api/mes/workorders/[id]/status`, `GET /api/mes/bom-templates`
- Produces: `<WorkOrderTable workOrders onStatusChange onPickClick />`, `<WorkOrderCreateModal onCreated />`

- [ ] **Step 1: `src/app/(dashboard)/mes/WorkOrderTable.tsx` 작성**

```tsx
"use client";

import type { WorkOrderDoc, WorkOrderStatus } from "@/lib/db";

const STATUS_STYLE: Record<WorkOrderStatus, string> = {
  QUEUED:        "bg-gray-100 text-gray-600",
  MATERIAL_WAIT: "bg-amber-100 text-amber-700",
  RUNNING:       "bg-blue-100 text-blue-700",
  DONE:          "bg-green-100 text-green-700",
  HOLD:          "bg-red-100 text-red-700",
};

const STATUS_LABEL: Record<WorkOrderStatus, string> = {
  QUEUED:        "대기",
  MATERIAL_WAIT: "자재 대기",
  RUNNING:       "실행 중",
  DONE:          "완료",
  HOLD:          "홀드",
};

const ROW_BG: Record<WorkOrderStatus, string> = {
  QUEUED:        "",
  MATERIAL_WAIT: "bg-amber-50",
  RUNNING:       "bg-blue-50",
  DONE:          "",
  HOLD:          "",
};

export default function WorkOrderTable({
  workOrders,
  onStatusChange,
  onPickClick,
}: {
  workOrders: WorkOrderDoc[];
  onStatusChange: (id: string, status: WorkOrderStatus) => Promise<void>;
  onPickClick: (wo: WorkOrderDoc) => void;
}) {
  if (workOrders.length === 0) {
    return (
      <div className="bg-white rounded-2xl shadow-sm p-12 text-center text-sm" style={{ color: "var(--text-3)" }}>
        작업지시가 없습니다. 상단 "+ 작업지시 생성" 버튼으로 추가하세요.
      </div>
    );
  }

  return (
    <div className="bg-white rounded-2xl shadow-sm overflow-hidden">
      <table className="w-full text-sm">
        <thead>
          <tr className="border-b" style={{ borderColor: "var(--border)" }}>
            {["WO 번호", "공정", "품목", "계획 수량", "상태", "생성일", "액션"].map(h => (
              <th key={h} className="px-4 py-3 text-left text-xs font-semibold" style={{ color: "var(--text-3)" }}>
                {h}
              </th>
            ))}
          </tr>
        </thead>
        <tbody>
          {workOrders.map(wo => (
            <tr
              key={wo._id}
              className={`border-b hover:opacity-90 ${ROW_BG[wo.status]}`}
              style={{ borderColor: "var(--border)" }}
            >
              {/* RUNNING 행: 좌측 accent */}
              {wo.status === "RUNNING" && (
                <td className="w-0.5 p-0 bg-[#0078D4]" />
              )}
              <td className="px-4 py-3 font-mono text-xs" style={{ color: "var(--text-2)" }}>
                {wo._id}
              </td>
              <td className="px-4 py-3 font-medium" style={{ color: "var(--text-1)" }}>
                {wo.processCode}
              </td>
              <td className="px-4 py-3" style={{ color: "var(--text-2)" }}>
                {wo.product}
              </td>
              <td className="px-4 py-3" style={{ color: "var(--text-2)" }}>
                {wo.plannedQty}런
              </td>
              <td className="px-4 py-3">
                <span className={`inline-block px-2 py-0.5 rounded-full text-xs font-medium ${STATUS_STYLE[wo.status]}`}>
                  {STATUS_LABEL[wo.status]}
                </span>
              </td>
              <td className="px-4 py-3 text-xs" style={{ color: "var(--text-3)" }}>
                {new Date(wo.createdAt).toLocaleDateString("ko-KR")}
              </td>
              <td className="px-4 py-3">
                <div className="flex gap-2">
                  {wo.status === "MATERIAL_WAIT" && (
                    <button
                      onClick={() => onPickClick(wo)}
                      className="px-3 py-1 text-xs bg-[#0078D4] text-white rounded-lg hover:bg-blue-700"
                    >
                      자재 피킹
                    </button>
                  )}
                  {wo.status === "QUEUED" && (
                    <button
                      onClick={() => onStatusChange(wo._id, "RUNNING")}
                      className="px-3 py-1 text-xs bg-green-600 text-white rounded-lg hover:bg-green-700"
                    >
                      실행 시작
                    </button>
                  )}
                  {wo.status === "RUNNING" && (
                    <button
                      onClick={() => onStatusChange(wo._id, "DONE")}
                      className="px-3 py-1 text-xs bg-gray-600 text-white rounded-lg hover:bg-gray-700"
                    >
                      완료
                    </button>
                  )}
                  {(wo.status === "QUEUED" || wo.status === "RUNNING") && (
                    <button
                      onClick={() => onStatusChange(wo._id, "HOLD")}
                      className="px-3 py-1 text-xs bg-red-100 text-red-700 rounded-lg hover:bg-red-200"
                    >
                      홀드
                    </button>
                  )}
                  {wo.status === "HOLD" && (
                    <button
                      onClick={() => onStatusChange(wo._id, "QUEUED")}
                      className="px-3 py-1 text-xs bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200"
                    >
                      해제
                    </button>
                  )}
                </div>
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>
  );
}
```

- [ ] **Step 2: `src/app/(dashboard)/mes/WorkOrderCreateModal.tsx` 작성**

```tsx
"use client";

import { useEffect, useState } from "react";
import type { Product } from "@/lib/db";

type BomLine = { materialId: string; qtyPerRun: number };
type Template = { _id: string; processCode: string; product: Product; lines: BomLine[] };

const PRODUCTS: Product[] = ["HBM", "DRAM", "NAND"];

export default function WorkOrderCreateModal({
  onClose,
  onCreated,
}: {
  onClose: () => void;
  onCreated: () => void;
}) {
  const [templates, setTemplates] = useState<Template[]>([]);
  const [processCode, setProcessCode] = useState("");
  const [product, setProduct] = useState<Product>("HBM");
  const [plannedQty, setPlannedQty] = useState("");
  const [submitting, setSubmitting] = useState(false);

  useEffect(() => {
    fetch("/api/mes/bom-templates").then(r => r.json()).then(setTemplates);
  }, []);

  const processCodes = Array.from(new Set(templates.map(t => t.processCode))).sort();
  const selectedTemplate = templates.find(t => t.processCode === processCode && t.product === product);

  const handleSubmit = async () => {
    if (!processCode || !plannedQty || Number(plannedQty) <= 0) return;
    setSubmitting(true);
    try {
      const r = await fetch("/api/mes/workorders", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ processCode, product, plannedQty: Number(plannedQty) }),
      });
      if (r.ok) { onCreated(); onClose(); }
    } finally {
      setSubmitting(false);
    }
  };

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center" style={{ backgroundColor: "rgba(0,0,0,0.4)" }}>
      <div className="bg-white rounded-2xl shadow-xl w-full max-w-lg mx-4 p-6">
        <div className="flex items-center justify-between mb-5">
          <h2 className="text-base font-bold" style={{ color: "var(--text-1)" }}>작업지시 생성</h2>
          <button onClick={onClose} className="text-gray-400 hover:text-gray-600 text-lg">×</button>
        </div>

        <div className="space-y-4">
          <div className="grid grid-cols-2 gap-3">
            <div>
              <label className="block text-xs font-medium mb-1" style={{ color: "var(--text-2)" }}>공정</label>
              <select
                value={processCode}
                onChange={e => setProcessCode(e.target.value)}
                className="w-full px-3 py-2 text-sm border rounded-lg"
                style={{ borderColor: "var(--border)" }}
              >
                <option value="">선택</option>
                {processCodes.map(p => <option key={p} value={p}>{p}</option>)}
              </select>
            </div>
            <div>
              <label className="block text-xs font-medium mb-1" style={{ color: "var(--text-2)" }}>품목</label>
              <select
                value={product}
                onChange={e => setProduct(e.target.value as Product)}
                className="w-full px-3 py-2 text-sm border rounded-lg"
                style={{ borderColor: "var(--border)" }}
              >
                {PRODUCTS.map(p => <option key={p} value={p}>{p}</option>)}
              </select>
            </div>
          </div>

          <div>
            <label className="block text-xs font-medium mb-1" style={{ color: "var(--text-2)" }}>계획 수량 (런)</label>
            <input
              type="number"
              value={plannedQty}
              onChange={e => setPlannedQty(e.target.value)}
              placeholder="예: 5"
              className="w-full px-3 py-2 text-sm border rounded-lg"
              style={{ borderColor: "var(--border)" }}
            />
          </div>

          {selectedTemplate && (
            <div>
              <div className="text-xs font-medium mb-2" style={{ color: "var(--text-2)" }}>BOM 자재 소요량 (런당 기준)</div>
              <div className="space-y-1 max-h-40 overflow-y-auto">
                {selectedTemplate.lines.map(l => (
                  <div key={l.materialId} className="flex justify-between text-xs px-3 py-1.5 bg-gray-50 rounded-lg">
                    <span style={{ color: "var(--text-2)" }}>{l.materialId}</span>
                    <span className="font-medium" style={{ color: "var(--text-1)" }}>
                      {l.qtyPerRun} × {plannedQty || "N"} = {plannedQty ? (l.qtyPerRun * Number(plannedQty)).toFixed(1) : "—"}
                    </span>
                  </div>
                ))}
              </div>
            </div>
          )}
        </div>

        <div className="flex gap-3 mt-6">
          <button onClick={onClose} className="flex-1 py-2 text-sm font-medium rounded-lg bg-gray-100 text-gray-700 hover:bg-gray-200">
            취소
          </button>
          <button
            onClick={handleSubmit}
            disabled={!processCode || !plannedQty || submitting}
            className="flex-1 py-2 text-sm font-medium rounded-lg bg-[#0078D4] text-white hover:bg-blue-700 disabled:opacity-50"
          >
            {submitting ? "생성 중..." : "작업지시 생성"}
          </button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: `MesClient.tsx`에 WorkOrderTable + Modal 연결**

기존 `MesClient.tsx` 전체를 아래로 교체:

```tsx
"use client";

import { useState, useCallback } from "react";
import type { WorkOrderDoc, WorkOrderStatus } from "@/lib/db";
import ProcessReadinessMatrix from "./ProcessReadinessMatrix";
import WorkOrderTable from "./WorkOrderTable";
import WorkOrderCreateModal from "./WorkOrderCreateModal";

type Tab = "readiness" | "workorders" | "log";

export default function MesClient({
  initialWorkOrders,
}: {
  initialWorkOrders: WorkOrderDoc[];
}) {
  const [tab, setTab] = useState<Tab>("readiness");
  const [workOrders, setWorkOrders] = useState<WorkOrderDoc[]>(initialWorkOrders);
  const [showCreateModal, setShowCreateModal] = useState(false);

  const TAB_LABELS: { key: Tab; label: string }[] = [
    { key: "readiness", label: "공정 준비 현황" },
    { key: "workorders", label: "작업지시 목록" },
    { key: "log", label: "실행 로그" },
  ];

  const refreshWorkOrders = useCallback(async () => {
    const r = await fetch("/api/mes/workorders");
    if (r.ok) setWorkOrders(await r.json());
  }, []);

  const handleStatusChange = useCallback(async (id: string, status: WorkOrderStatus) => {
    await fetch(`/api/mes/workorders/${id}/status`, {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ status }),
    });
    await refreshWorkOrders();
  }, [refreshWorkOrders]);

  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h1 className="text-xl font-bold" style={{ color: "var(--text-1)" }}>
          공정 실행 관리 (MES)
        </h1>
        <button
          className="px-4 py-2 bg-[#0078D4] text-white text-sm font-medium rounded-lg hover:bg-blue-700"
          onClick={() => setShowCreateModal(true)}
        >
          + 작업지시 생성
        </button>
      </div>

      <div className="flex gap-1 border-b" style={{ borderColor: "var(--border)" }}>
        {TAB_LABELS.map(({ key, label }) => (
          <button
            key={key}
            onClick={() => setTab(key)}
            className={`px-4 py-2 text-sm font-medium border-b-2 transition-colors ${
              tab === key ? "border-[#0078D4] text-[#0078D4]" : "border-transparent"
            }`}
            style={{ color: tab === key ? "#0078D4" : "var(--text-2)" }}
          >
            {label}
          </button>
        ))}
      </div>

      <div>
        {tab === "readiness" && (
          <ProcessReadinessMatrix
            onCellClick={(processCode, product, materialId) => {
              console.log("셀 클릭:", processCode, product, materialId);
              // Task 9에서 PickingDrawer 연결
            }}
          />
        )}
        {tab === "workorders" && (
          <WorkOrderTable
            workOrders={workOrders}
            onStatusChange={handleStatusChange}
            onPickClick={(wo) => {
              console.log("피킹 클릭:", wo._id);
              // Task 9에서 PickingDrawer 연결
            }}
          />
        )}
        {tab === "log" && (
          <div className="text-sm text-center py-16" style={{ color: "var(--text-3)" }}>
            실행 로그 (Task 9에서 구현)
          </div>
        )}
      </div>

      {showCreateModal && (
        <WorkOrderCreateModal
          onClose={() => setShowCreateModal(false)}
          onCreated={refreshWorkOrders}
        />
      )}
    </div>
  );
}
```

- [ ] **Step 4: 브라우저에서 확인**

`http://localhost:3000/mes` → "작업지시 목록" 탭:
1. WO 테이블 표시 확인
2. "+ 작업지시 생성" 클릭 → 모달 오픈 확인
3. 공정 선택 → BOM 자재 소요량 자동 표시 확인
4. 수량 입력 → 총 소요량 계산 확인 (qtyPerRun × plannedQty)
5. "작업지시 생성" 클릭 → QUEUED 상태로 목록에 추가 확인
6. "실행 시작" 클릭 → RUNNING 상태 + 파란 배경 확인
7. "완료" 클릭 → DONE 상태 확인

- [ ] **Step 5: TypeScript 컴파일 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 6: 커밋**

```bash
git add "src/app/(dashboard)/mes/WorkOrderTable.tsx" "src/app/(dashboard)/mes/WorkOrderCreateModal.tsx" "src/app/(dashboard)/mes/MesClient.tsx"
git commit -m "feat(mes): add WorkOrderTable with status actions and WorkOrderCreateModal"
```

---

### Task 9: PickingDrawer

**Files:**
- Create: `src/app/(dashboard)/mes/PickingDrawer.tsx`
- Modify: `src/app/(dashboard)/mes/MesClient.tsx`

**Interfaces:**
- Consumes: `WorkOrderDoc`, `BomLine`, `GET /api/mes/process-readiness` (lot 목록), `POST /api/mes/workorders/[id]/pick`
- Produces: `<PickingDrawer wo onClose onPicked />` — 오른쪽 슬라이드인 드로어

useSession에서 role을 읽어 MATERIALS | ADMIN만 피킹 확정 버튼 활성화. 다른 role은 `title="자재관리팀 권한 필요"` tooltip + disabled.

- [ ] **Step 1: `src/app/(dashboard)/mes/PickingDrawer.tsx` 작성**

```tsx
"use client";

import { useEffect, useState } from "react";
import { useSession } from "next-auth/react";
import type { WorkOrderDoc, BomLine, InventoryLotDoc } from "@/lib/db";

type PickState = {
  lotId: string;
  qty: string;
};

function LotRow({
  lot,
  pickState,
  onChange,
}: {
  lot: InventoryLotDoc;
  pickState: PickState | undefined;
  onChange: (lotId: string, qty: string) => void;
}) {
  const dDay = lot.expiryDate
    ? Math.round((new Date(lot.expiryDate).getTime() - Date.now()) / 86400000)
    : null;

  return (
    <div className="flex items-center gap-2 p-2 bg-gray-50 rounded-lg text-xs">
      <div className="flex-1 min-w-0">
        <div className="font-medium truncate" style={{ color: "var(--text-1)" }}>{lot.lotNo}</div>
        <div style={{ color: "var(--text-3)" }}>
          가용 {lot.availableQuantity.toFixed(1)}
          {dDay !== null && <span className="ml-1.5">· D{dDay >= 0 ? "+" : ""}{dDay}</span>}
        </div>
      </div>
      <input
        type="number"
        value={pickState?.qty ?? ""}
        onChange={e => onChange(lot._id, e.target.value)}
        placeholder="수량"
        max={lot.availableQuantity}
        className="w-20 px-2 py-1 border rounded-lg text-xs"
        style={{ borderColor: "var(--border)" }}
      />
    </div>
  );
}

export default function PickingDrawer({
  wo,
  onClose,
  onPicked,
}: {
  wo: WorkOrderDoc;
  onClose: () => void;
  onPicked: () => void;
}) {
  const { data: session } = useSession();
  const role = (session?.user as { role?: string })?.role ?? "";
  const canPick = role === "MATERIALS" || role === "ADMIN";

  const [selectedLine, setSelectedLine] = useState<BomLine>(wo.bomLines[0]);
  const [lots, setLots] = useState<InventoryLotDoc[]>([]);
  const [pickStates, setPickStates] = useState<Record<string, string>>({});
  const [submitting, setSubmitting] = useState(false);

  useEffect(() => {
    if (!selectedLine) return;
    fetch(`/api/lots?materialId=${selectedLine.materialId}`)
      .then(r => r.ok ? r.json() : [])
      .then(setLots)
      .catch(() => setLots([]));
  }, [selectedLine?.materialId]);

  const handlePick = async () => {
    setSubmitting(true);
    try {
      for (const [lotId, qtyStr] of Object.entries(pickStates)) {
        const qty = Number(qtyStr);
        if (!qty || qty <= 0) continue;
        await fetch(`/api/mes/workorders/${wo._id}/pick`, {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ materialId: selectedLine.materialId, lotId, qty }),
        });
      }
      onPicked();
      onClose();
    } finally {
      setSubmitting(false);
    }
  };

  const totalPicking = Object.values(pickStates).reduce((s, v) => s + (Number(v) || 0), 0);

  return (
    <>
      {/* 배경 오버레이 */}
      <div className="fixed inset-0 z-40" onClick={onClose} />

      {/* 드로어 */}
      <div className="fixed right-0 top-0 h-full w-[440px] z-50 bg-white shadow-xl flex flex-col">
        {/* 헤더 */}
        <div className="flex items-start justify-between p-5 border-b" style={{ borderColor: "var(--border)" }}>
          <div>
            <div className="text-sm font-bold" style={{ color: "var(--text-1)" }}>자재 피킹</div>
            <div className="text-xs mt-0.5" style={{ color: "var(--text-3)" }}>{wo._id} · {wo.processCode} · {wo.product}</div>
          </div>
          <button onClick={onClose} className="text-gray-400 hover:text-gray-600 text-lg mt-0.5">×</button>
        </div>

        {/* BOM 자재 선택 탭 */}
        <div className="flex gap-1 px-4 pt-3 pb-0 overflow-x-auto">
          {wo.bomLines.map(line => {
            const fulfilled = (line.actualQty ?? 0) >= line.plannedQty;
            return (
              <button
                key={line.materialId}
                onClick={() => { setSelectedLine(line); setPickStates({}); }}
                className={`px-3 py-1.5 text-xs rounded-full whitespace-nowrap font-medium border transition-colors ${
                  selectedLine?.materialId === line.materialId
                    ? "bg-[#0078D4] text-white border-[#0078D4]"
                    : fulfilled
                    ? "bg-green-100 text-green-700 border-green-200"
                    : "bg-gray-100 text-gray-600 border-gray-200"
                }`}
              >
                {line.materialId}
                {fulfilled ? " ✓" : ` (${line.plannedQty - (line.actualQty ?? 0)}↓)`}
              </button>
            );
          })}
        </div>

        {/* 선택된 자재 정보 */}
        {selectedLine && (
          <div className="px-4 py-3 border-b" style={{ borderColor: "var(--border)" }}>
            <div className="text-xs" style={{ color: "var(--text-3)" }}>
              계획 {selectedLine.plannedQty} / 피킹완료 {selectedLine.actualQty ?? 0} / 잔여 {selectedLine.plannedQty - (selectedLine.actualQty ?? 0)}
            </div>
          </div>
        )}

        {/* Lot 목록 */}
        <div className="flex-1 overflow-y-auto px-4 py-3 space-y-2">
          {lots.length === 0 ? (
            <div className="text-xs text-center py-8" style={{ color: "var(--text-3)" }}>
              가용 Lot 없음
            </div>
          ) : (
            lots
              .filter(l => l.qualityStatus === "AVAILABLE" && l.availableQuantity > 0)
              .sort((a, b) => {
                if (a.expiryDate && b.expiryDate)
                  return new Date(a.expiryDate).getTime() - new Date(b.expiryDate).getTime();
                if (a.expiryDate) return -1;
                if (b.expiryDate) return 1;
                return new Date(a.receivedAt).getTime() - new Date(b.receivedAt).getTime();
              })
              .map(lot => (
                <LotRow
                  key={lot._id}
                  lot={lot}
                  pickState={pickStates[lot._id] ? { lotId: lot._id, qty: pickStates[lot._id] } : undefined}
                  onChange={(lotId, qty) => setPickStates(prev => ({ ...prev, [lotId]: qty }))}
                />
              ))
          )}
        </div>

        {/* 피킹 확정 */}
        <div className="p-4 border-t space-y-3" style={{ borderColor: "var(--border)" }}>
          <div className="flex justify-between text-xs" style={{ color: "var(--text-2)" }}>
            <span>총 피킹 수량</span>
            <span className="font-semibold">{totalPicking}</span>
          </div>
          <div className="flex gap-3">
            <button onClick={onClose} className="flex-1 py-2 text-sm font-medium rounded-lg bg-gray-100 text-gray-700 hover:bg-gray-200">
              취소
            </button>
            <button
              onClick={handlePick}
              disabled={!canPick || totalPicking <= 0 || submitting}
              title={!canPick ? "자재관리팀 권한 필요" : undefined}
              className="flex-1 py-2 text-sm font-medium rounded-lg bg-[#0078D4] text-white hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed"
            >
              {submitting ? "처리 중..." : "피킹 확정"}
            </button>
          </div>
          {!canPick && (
            <div className="text-xs text-center" style={{ color: "var(--text-3)" }}>
              자재관리팀(MATERIALS) 또는 관리자 권한이 필요합니다.
            </div>
          )}
        </div>
      </div>
    </>
  );
}
```

- [ ] **Step 2: `/api/lots` route 확인 — 없으면 신규 생성**

```bash
ls /Users/hwangjihun/Fab/fab-system/src/app/api/lots/
```

`route.ts`가 있는지 확인. 없으면:

```ts
// src/app/api/lots/route.ts (없을 경우에만 신규 생성)
import { NextRequest, NextResponse } from "next/server";
import { collections } from "@/lib/db";

export const dynamic = "force-dynamic";

export async function GET(req: NextRequest) {
  const { searchParams } = req.nextUrl;
  const materialId = searchParams.get("materialId");
  const { inventoryLots } = await collections();
  const filter = materialId ? { materialId } : {};
  const lots = await inventoryLots
    .find(filter)
    .sort({ expiryDate: 1, receivedAt: 1 })
    .limit(50)
    .toArray();
  return NextResponse.json(lots);
}
```

- [ ] **Step 3: `MesClient.tsx`에 PickingDrawer 연결**

`MesClient.tsx`에서:

import 추가:
```tsx
import PickingDrawer from "./PickingDrawer";
```

state 추가 (기존 `showCreateModal` 아래):
```tsx
const [pickingWo, setPickingWo] = useState<WorkOrderDoc | null>(null);
```

ProcessReadinessMatrix의 `onCellClick` 핸들러 교체:
```tsx
onCellClick={(_processCode, _product, _materialId) => {
  setTab("workorders");
}}
```

WorkOrderTable의 `onPickClick` 핸들러 교체:
```tsx
onPickClick={(wo) => setPickingWo(wo)}
```

`showCreateModal` 조건부 렌더링 아래에 추가:
```tsx
{pickingWo && (
  <PickingDrawer
    wo={pickingWo}
    onClose={() => setPickingWo(null)}
    onPicked={refreshWorkOrders}
  />
)}
```

- [ ] **Step 4: 브라우저에서 확인**

`http://localhost:3000/mes` → "작업지시 목록" 탭:
1. MATERIAL_WAIT 상태 WO가 있으면 "자재 피킹" 버튼 클릭 → 드로어 오른쪽 슬라이드인 확인
2. BOM 자재 탭 전환 확인
3. Lot 목록 FEFO 순 표시 확인 (expiryDate 짧은 것 먼저)
4. 수량 입력 → "피킹 확정" 클릭 → lot 차감 + WO bomLine actualQty 업데이트 확인
5. MATERIALS 권한이 아닌 사용자: 버튼 비활성화 + 안내 메시지 확인

(MATERIAL_WAIT WO가 없으면: 먼저 WO 생성 → "실행 시작" 대신 PATCH로 MATERIAL_WAIT로 직접 전이 테스트)
```bash
curl -X PATCH http://localhost:3000/api/mes/workorders/WO-XXXXX/status \
  -H "Content-Type: application/json" \
  -d '{"status":"MATERIAL_WAIT"}'
```

- [ ] **Step 5: TypeScript 컴파일 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1 | head -20
```

- [ ] **Step 6: 커밋**

```bash
git add "src/app/(dashboard)/mes/PickingDrawer.tsx" "src/app/(dashboard)/mes/MesClient.tsx"
git commit -m "feat(mes): add PickingDrawer with FEFO lot selection and role-gated confirm"
```

---

### Task 10: 실행 로그 탭 + 최종 연결

**Files:**
- Modify: `src/app/(dashboard)/mes/MesClient.tsx`

**Interfaces:**
- Consumes: `GET /api/mes/workorders` (전체 WO 목록 — DONE 포함), `InventoryMovementDoc` (ISSUE type + processCode)
- Produces: 시간순 역정렬 실행 로그 피드

- [ ] **Step 1: `MesClient.tsx`의 log 탭 콘텐츠 구현**

MesClient.tsx에서 log 탭 부분 교체:

state 추가:
```tsx
const [logEntries, setLogEntries] = useState<WorkOrderDoc[]>([]);
const [logLoaded, setLogLoaded] = useState(false);
```

tab이 "log"로 변경될 때 데이터 로드 — useEffect 추가:
```tsx
useEffect(() => {
  if (tab === "log" && !logLoaded) {
    fetch("/api/mes/workorders")
      .then(r => r.json())
      .then((data: WorkOrderDoc[]) => {
        setLogEntries(data);
        setLogLoaded(true);
      });
  }
}, [tab, logLoaded]);
```

log 탭 콘텐츠 교체:
```tsx
{tab === "log" && (
  <div className="bg-white rounded-2xl shadow-sm p-4">
    <h3 className="text-sm font-semibold mb-3" style={{ color: "var(--text-1)" }}>작업지시 실행 이력</h3>
    <div className="space-y-2 max-h-[600px] overflow-y-auto">
      {logEntries.length === 0 && (
        <p className="text-sm text-center py-12" style={{ color: "var(--text-3)" }}>이력 없음</p>
      )}
      {[...logEntries].sort((a, b) => new Date(b.updatedAt).getTime() - new Date(a.updatedAt).getTime()).map(wo => (
        <div key={wo._id} className="flex items-start gap-3 py-2 border-b last:border-0 text-xs" style={{ borderColor: "var(--border)" }}>
          <div className="w-2 h-2 rounded-full mt-1 shrink-0" style={{
            backgroundColor: wo.status === "DONE" ? "#00B96B" : wo.status === "RUNNING" ? "#0078D4" : wo.status === "HOLD" ? "#EA002C" : "#9CA3AF"
          }} />
          <div className="flex-1 min-w-0">
            <div className="font-medium" style={{ color: "var(--text-1)" }}>{wo._id}</div>
            <div style={{ color: "var(--text-3)" }}>
              {wo.processCode} · {wo.product} · {wo.plannedQty}런 · {wo.status}
            </div>
          </div>
          <div className="shrink-0" style={{ color: "var(--text-3)" }}>
            {new Date(wo.updatedAt).toLocaleString("ko-KR", { month: "2-digit", day: "2-digit", hour: "2-digit", minute: "2-digit" })}
          </div>
        </div>
      ))}
    </div>
  </div>
)}
```

- [ ] **Step 2: ProcessReadinessMatrix 셀 클릭 시 MATERIAL_WAIT WO 자동 생성 + 피킹 드로어 오픈**

MesClient.tsx의 ProcessReadinessMatrix `onCellClick` 핸들러 최종 교체:
```tsx
onCellClick={(processCode, product, _materialId) => {
  // 해당 공정+품목의 MATERIAL_WAIT WO가 있으면 피킹 드로어 바로 오픈
  const existingWo = workOrders.find(
    w => w.processCode === processCode && w.product === product && w.status === "MATERIAL_WAIT"
  );
  if (existingWo) {
    setPickingWo(existingWo);
  } else {
    // 없으면 작업지시 탭으로 이동
    setTab("workorders");
  }
}}
```

- [ ] **Step 3: 브라우저 전체 흐름 최종 확인**

`http://localhost:3000/mes` 접속:
1. "공정 준비 현황" 탭 — KPI 카드 + 매트릭스 정상 표시
2. 빨간/주황 셀 클릭 → 작업지시 탭으로 이동
3. "+ 작업지시 생성" → 공정 선택 → BOM 자재 목록 → 수량 입력 → 생성 → 목록 갱신
4. "실행 시작" → RUNNING 상태 (파란 배경 + 액션 버튼 변경)
5. "완료" → DONE 상태
6. "실행 로그" 탭 → 이력 표시
7. MATERIAL_WAIT 상태 WO에서 "자재 피킹" → 드로어 → 수량 입력 → 확정 → actualQty 반영

- [ ] **Step 4: TypeScript 컴파일 최종 확인**

```bash
cd /Users/hwangjihun/Fab/fab-system && npx tsc --noEmit 2>&1
```

Expected: 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add "src/app/(dashboard)/mes/MesClient.tsx"
git commit -m "feat(mes): add execution log tab and matrix cell click integration"
```

---

## 완료 체크리스트

- [ ] Task 1: WorkOrderDoc, BomTemplateDoc DB 타입
- [ ] Task 2: BOM 시드 스크립트 + GET /api/mes/bom-templates
- [ ] Task 3: GET /api/mes/process-readiness (DOH 계산)
- [ ] Task 4: GET/POST /api/mes/workorders + PATCH status 전이
- [ ] Task 5: POST /api/mes/workorders/[id]/pick (FEFO 피킹)
- [ ] Task 6: 사이드바 "생산 실행" 그룹 + /mes 페이지 스캐폴드
- [ ] Task 7: ProcessReadinessMatrix (DOH 색상 + KPI 카드)
- [ ] Task 8: WorkOrderTable + WorkOrderCreateModal
- [ ] Task 9: PickingDrawer (role-gated)
- [ ] Task 10: 실행 로그 탭 + 전체 흐름 연결
